/** Original NCDAI artwork, reused unchanged with the user's authorization. */
export function BrandLogo({ className = '' }: { className?: string }) {
  return <img src="/brand/ncdai-logo.png" className={`brand-logo ${className}`} alt="" width={48} height={48} />
}
